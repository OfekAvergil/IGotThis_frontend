import {IP, PORT} from '@env'

export const Colors = {
    primary: '#8EA7E9',
    secondery: '#7286D3',
    light: '#E5E0FF',
    basicGrey: '#C6C0D2',
    background: 'white',
    pink: "#E45082",
}
//const {IP, PORT} = process.env
export const BASE_URL = `http://${IP}:${PORT}`;
